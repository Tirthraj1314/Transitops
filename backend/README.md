# ⚠️ Superseded

This `backend` branch only implements auth (register/login/me/change-password).
The `KPI` branch has a complete backend — vehicles, drivers, trips, maintenance,
fuel, expenses, dashboard, reports, RBAC on every route, and a trip
dispatch/complete/cancel flow that matches `docs/SPEC.md`'s business rules.

**Build on `KPI`, not this branch.** This one is kept around for reference only.
